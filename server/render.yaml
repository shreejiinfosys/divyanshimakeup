services:
  - type: web
    name: divyanshi-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: CORS_ORIGIN
        sync: false   # set in Render dashboard: your client + admin URLs, comma-separated
      - key: PORT
        value: 5000
